var Game = (function() {

    var defaults = {

    };

    function Construct(_options) {
        this.options = $.extend({}, defaults, _options);
    }

    Construct.prototype = {

    };

    return Construct;

})();